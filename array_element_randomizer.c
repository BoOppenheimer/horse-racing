
#include <string.h> // for memory
#include <stdio.h>
#include "array_element_randomizer.h"

// takes an array and randomizes the element location
// will use pointers since we do not know the type of array element, nore the size of array passed

// void array_element_randomizer (void *array, size_t elementSize, size_t elementCount, void (*func)(void *)) {
   
// }


int array_element_randomizer (void *array[], size_t elementSize, size_t elementCount, void (*func)(void *)) {
  
}